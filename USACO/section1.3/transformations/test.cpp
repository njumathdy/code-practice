#include <iostream>
#include <fstream>
#include <string>
#include <vector>
#include <list>
#include <forward_list>
#include <map>
#include <deque>
#include <algorithm>

using namespace std;

int main() {

    vector<int> x(10, 0);
    for(int i = 0; i < 10; i++) {
        cout << x[i] << endl;
    }
    cout << 101 / 10 << endl;

    return 0;
}
